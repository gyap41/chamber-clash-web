# Claudeへの引き継ぎ：リナの走行モーション

区分: 一時メモ（作業引き継ぎ）  
状態: Claudeが引継ぎ済み。v4（評価済み・弾み不足）、v5（方針未達）、v6（仮胴体）、v7（側面、本編で違和感少ないと確認）、v8（8方向、ユーザー確認待ち）。いずれも未採用  
更新日: 2026-09-26  
範囲: リナの体型・素材・移動アニメーション試作。ゲーム本編への接続は未実施  
反映先: [制作記録](../art/reviews/rina-parts-2026-09-25/README.md)、採用後は[キャラ美術](../design/CHARACTER_BIBLE.md)と[Actor表示](../development/ACTOR_ANIMATION.md)  
終了条件: 後任が現状を確認し、制作結果・未対応を制作記録と必要な正本へ反映してこのメモを閉じる

## 最初に把握すること

作業ディレクトリは `C:\GameCreate\chamber-clash`。AGENTS.md、docs/README.md、docs/DOCUMENTATION_GUIDE.mdに従う。今回のユーザー依頼は「claudeに作業を引き継がせたい」。引き継ぎ資料を準備した段階で、Claudeの起動や外部サービスへの送信はしていない。

ユーザーは何度も機械的な動きを指摘している。再生できる・数値テストが通ることを美術品質の合格として報告しない。最新版の生成8ポーズも不合格。既存の未採用素材を無理に動かし続けるより、必要なら素材から作り直してほしいという希望。直前に「生成してよいです」と明示許可がある。無制限の生成や別の有料API・認証設定変更の許可と拡大解釈しない。

## ユーザーが求める見た目と動き

- 参考はガンナーちゃんのゲーム画像・動画。約2頭身でよく、脚や首を伸ばした成人寄りの体型にはしない。「ずんぐりむっくり」の修正は頭身を上げる意味ではない。
- 大きい頭、小さく細めの胴、短い手足。首はほぼ見えず顎からスカーフへつながる。
- リナの識別点は栗色の髪と跳ね毛、真鍮のゴーグル・青緑のレンズ、青緑の目、アイボリーの服、橙色スカーフ、茶色の手袋・靴。
- エリアを動くアクションゲーム。通常移動は走るイメージ。銃を構えつつ重心が上下し、接地・荷重・蹴り出し・回収が全身でつながる動きが必要。
- 左右と後ろ向きも必要。移動方向と照準方向は独立する。右側面だけの試作を全方向完成と扱わない。

## 素材と現状

以下のファイルはすべて `docs/art/reviews/rina-parts-2026-09-25/` 内。詳細・生成プロンプトは同フォルダーREADME。

|ファイル|用途・評価|
|---|---|
|turnaround.png|正面・右側面・背面の体型参照。方向別試作であり採用済みではない|
|run-keyposes.png|最新の全身8姿勢の初回生成。1774×887 RGBA透過|
|run-keyposes-fix.png|脚の役割交代を修正生成したもの。なお不合格|
|run-keyposes-review.html / run-keyposes-fix-review.html|8姿勢の仮再生。通常サイズ約56px・拡大・停止・コマ送り・低速・左右反転|
|build_keyposes_review.py|上記HTMLを生成。原画を内包し横4×縦2の等分セル、共通倍率・セル原点で表示|
|run-keyposes-prompt.txt / run-keyposes-fix-prompt.txt|最新2回の生成指示全文|
|reference-motion.html|旧パーツ方式のエリア移動試作。WASD、左右照準、後退を比較できる。ユーザー評価は機械的|
|build_reference_motion.py / run-review.template.html|旧方式の再現元。周期やIK調整を繰り返しても品質未達|
|run-parts-key.png|旧方式の分割素材。マゼンタをCanvasで除去して使用|

最新8姿勢は全身の沈み込みが出たが、近側の脚を前へ出す形が両半周期に残る。左右脚の接地交代が成立せず、片足で跳ねるように見える。修正生成でも改善不足。頭・髪・銃の形状がコマ間で微妙に変わる。初回と修正で合計2回、Codex内蔵imagegenを使用。追加の生成API設定変更なし。

旧パーツ方式は上体がほぼ固定、脚画像を伸縮・移動し、上下揺れを別に加えていた。周期0.36秒から0.52秒へ遅くし、足上げやIKを調整したがユーザーの違和感は解消していない。数値調整のみへ戻らないこと。

プレビューは本編と別。正面・背面の銃構え、専用走行、自由角度の照準、射撃・回避、ゲーム速度との同期は未完。既存ゲーム素材を置換していない。

## 参考動画を確認する方法

ユーザー原本: `C:\Users\ikare\Desktop\レコーディング 2026-09-25 154654.mp4`

ローカルコピー: `.local/reference-video/reference.mp4`（存在確認済み）。21.9秒、1252×782、H.264 656フレーム。録画は正常に動いている。

以前ブラウザーのseek後にcanvas抽出した際、同一フレームが返り「静止」と誤判断した。PyAVで直接デコードすると動作を確認できた。ブラウザー抽出の古い結果を根拠にしない。

- `.local/reference-video/direct-sheet.jpg`: 全体の時間サンプル。
- `.local/reference-video/run-detail.jpg`: 8.5〜9.6秒を0.1秒ごとに抽出した拡大一覧（存在確認済み）。
- `.local/reference-video/detail-00.png`〜`detail-11.png`: 各拡大フレーム。
- `.local/reference-video/frame-00.png`〜`frame-11.png`: 全体の抽出画像。

8.5〜9.5秒付近に右移動中の構え直しが見える。参考ゲームがボーン式かコマ式かは未確定。画面で確認できる動きだけを根拠にする。

直接デコードに使用したPythonは `C:/Users/ikare/.cache/codex-runtimes/codex-primary-runtime/dependencies/python/python.exe`、PyAVは `.local/video-deps` に導入済み。Codexではこの依存先のアクセスに権限の問題があり、昇格実行時に成功した。後任の環境で使えるか再確認し、まず既存抽出画像を利用できる。動画・抽出画像は私的な参考でありゲーム配布素材には含めない。`.local` はGit除外のため別マシンへの引き継ぎでは原本の別途共有が必要。

## 次工程の提案（ユーザー採用済みの手法ではない）

1. 参考動画と旧試作を実際に比較し、接地・荷重・骨盤/胸・頭の追従を確認する。
2. まず近側脚と遠側脚それぞれが接地する対の姿勢を個別に確定する。8コマ一括生成を繰り返す前に、支持脚と重なりが交代するかレビューする。
3. 対になる姿勢が成立してから荷重、蹴り出し、回収を制作。頭と武器の形状安定性を保つ方法を選ぶ。全身コマ方式と分割方式のどちらかに無条件で固定しない。
4. 約56pxと拡大、等速と低速、コマ送りで確認する。勝手なコマ別拡縮や毎コマ靴底合わせで、必要な上下動を消さない。接地と空中を区別し、移動量と足送りの関係を確認する。
5. 側面の一周期が成立してから左右移動・後退・前後向きへ広げる。射撃・回避との接続は別途検証。採用前に全方向を量産しない。

## ローカル実行と作業差分

HTMLは画像内包なので直接開ける。HTTPが必要ならリポジトリルートで `python -m http.server 8766 --bind 127.0.0.1`。既存サーバーが残っている場合は重複起動しない。確認URLは `http://127.0.0.1:8766/docs/art/reviews/rina-parts-2026-09-25/run-keyposes-fix-review.html`。

初回キーポーズプレビューはブラウザー表示と停止操作を確認。修正生成版は同じビルダーで出力したが、自然な連続動作を合格としていない。直前の資料リンク検査は成功した。

リポジトリにはステージ・戦闘・探索等の多数の既存未コミット差分がある。キャラ試作フォルダー自体も未追跡。全差分をこの作業の成果と見なさず、reset・clean・一括上書きをしない。Gitの所有者警告が出る場合、この環境では `git -c safe.directory=C:/GameCreate/chamber-clash status --short` で確認できた。コミット・pushはしていない。

資料を追加・移動・削除したら分野別READMEを更新し、`python tools/docs_index.py` と `python tools/docs_index.py --check` を実行する。

## 引継ぎ後の進捗（Claude、2026-09-25）

- 参考動画を30fpsで直接デコードし、8.3〜9.3秒の足元を比較。上体固定のまま、脚を前後に開く姿勢と揃える姿勢が交互に出ることを確認。
- 生成を追加せず、最新修正生成の1コマから上体だけを切り出し、脚を近側/遠側別に手描き（ベクター）する方式で側面走行v4を制作。詳細・ファイル・未対応は[制作記録](../art/reviews/rina-parts-2026-09-25/README.md)の「側面走行サイクル v4」。
- v4へのユーザー評価：「大分よくなった」。ただし参考動画に比べて弾みが足りない。歩幅を現実の移動量と正確に合わせる必要はなく、もっとダイナミックでよい。原因は、v4の上下動（原画セルで最大7px）と滞空の持上げ（5px）が56px表示で約1px以下しかないこと。v4は未採用。（以前このメモに「別セッションで『まだ未評価・未採用』と回答を得た」と記していたが、ユーザーの説明と食い違うため訂正した。）
- v5：v4の1枚絵から手＋拳銃・跳ね毛・後方の髪房・スカーフを切り抜いて回す方式にとどまり、下記の合意方針1〜4を満たさなかった（2026-09-25のレビューで確認、詳細は制作記録「v5レビューの指摘」）。
- v6：v5レビューを受けて作り直し。本編の銃画像を grip で持たせ、腕をコードで描き、髪・スカーフは画像全体のワープで揺らし、弾みと脚表を作り直した。胴体はv4原画から銃と手を消した仮素材。詳細・数値は制作記録「側面走行サイクル v6」。
- 方針6の生成は、画像CLIのローカル検証（--check）が予算上限（使用量台帳78件、LIMIT_REQUESTS=79）で停止したため未実施。API送信なし。上限変更はユーザー承認が必要。
- 2026-09-26：ユーザーが v6 を見て弾みは「強」を選択。生成を許可し、画像CLIの上限を自ら79→82件に変更。Claudeの実行は自動モードの判定で拒否されたため、生成コマンドはユーザーが実行した（`rina-side-body-parts-v1`、1回、推定0.113ドル、使用79/82件）。
- v7：生成した頭＋胴・ポニーテール・スカーフの垂れで仮胴体を差し替え、両腕を肩からコードで描画。銃の大きさ・手の高さへの対策として、本編の大きさのまま銃を「胴の前・頭の後ろ」に描く描画順を提案・実装（比較用に本編と同じ順へ切替可）。詳細は制作記録「側面走行サイクル v7」。
- 2026-09-26 決定：リュックなし、スカーフの垂れは橙色で残す、見た目も8方向（描き分け5向き）、残り4向きの生成より先に側面v7を本編へ仮組み込み。側面の仮組み込みは当時 `scripts/visuals/rina_side_run.gd`（v8で `rina_run_rig.gd` に置き換え）、確認結果は制作記録冒頭「決定事項と本編への仮組み込み」。

### v5で合意した方針（2026-09-25、ユーザーと合意。v6以降も有効）

1. 体と武器を分離する。武器は本編と同じく別レイヤーにし、既存の `assets/first-workshop/equipment/guns/*.png` を `data/weapon_visuals.json` の grip に合わせて持たせる。
2. 腕はコードで描き、肩から武器の grip へ届かせる。長い武器は前の手の支持点を足して両手持ち。
3. 常に構える形を基本とし、体は弾ませ、銃は照準方向を保って少し遅れて追従させる（銃口は体ほど跳ねない）。
4. 弾みは「中」「強」の2案。56px表示で上下2〜3px、滞空を長く、着地で縮み跳躍で伸びる、脚を大きく開き後ろ足を高く跳ね上げる。
5. 髪・ポニーテール・スカーフを別パーツにして遅れて揺らす。
6. 腕と銃のない側面の頭＋胴体（ポニーテール・スカーフ先は別パーツ）を gpt-image-2 で生成してよい（--check 後に1回ずつ、最大3回、エラー時は再試行せず停止）。

- v8（2026-09-26）：残り4向きを生成（各1回、使用81/82件）し、5向きを組み立てて8方向で本編へ仮組み込み（`scripts/visuals/rina_run_rig.gd`、既定無効、V キーで切替）。詳細は制作記録「走行 v8」。
- 2026-09-26：ユーザーが8方向を確認し「いい感じになってきた」。待機動作と回避モーションの不足の指摘を受け、v8に待機（呼吸8コマ）と回避（6コマ）を追加。
- 2026-09-26：ユーザー要望「待機・移動中の髪とスカーフの揺れ」「ポニーテールの位置が高い」。比較から C（付け根を下げ後ろへ倒す）を選択。揺れはゲーム側の毎フレームのばね計算へ移行。
- 2026-09-26：ユーザー指摘（正面の足）と参考動画との比較レビューを受け、方針「動きはコード、止まった絵のパーツは生成」に合意。コードで正面・背面の足（ハの字）、待機の非対称、背面の銃を頭の上へ、背面のスカーフ、正面・背面の回避の傾きを修正済み。次は腕・手袋の生成（画像CLIの上限を82→86へ引き上げてもらう必要あり）、任意で靴の生成。
- 2026-09-26：腕・手袋を生成（rina-arm-parts-v2、v1は送信済み・画像未受領で台帳を解決）し置き換え。参考動画との再比較で背面の袖・斜め後ろの腕・手袋の大きさ・正面の長い武器を修正。キャラの大きさ案D（C キー）を試験用に追加。
- 2026-09-26：複数キャラ向けに一般化（設定ファイル＋共通ビルダー `tools/character_rig/`、描画 `character_rig8.gd`）。近接（ナイフ、rina-knife-v1）を追加し、当たり判定を左右70°へ（距離は52pxを試したが短いとの評価で64pxのまま）。次は2人目のキャラで手順を試す（生成約5回、枠は残り2件のため上限の引き上げが必要）。
- ユーザーの目視判断待ち（本編で V キーを押して確認）。採用後は回避の新素材化、他キャラへの展開を検討。この段階では本メモを閉じない。
