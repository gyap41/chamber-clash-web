# 生成SE16種のゲーム接続

既存sound.gdに16音源をpreload。shotの0/20は拳銃、23/26は重い単発、19/27/28は連射、3/6/7/36はエネルギー。それ以外の射撃と未制作イベントは既存合成音。既存の1発射イベントにつき1回の再生を維持し、追射や弾数分を新しく重ねない。
hit/dodge/reload/boomへ素材接続。準備操作の選択、決定、取消、配置、購入、失敗でUI音。プレビュー移動やrefreshでは発音しない。勝敗は結果確定時に既存再生を止めP1視点で1回、引分けは無音。
既定OFFとセッション内設定維持、16音巡回上限を保持。生成音は既存コンプレッサーバス、戦闘／結果-12dB、UI-18dB（既存volume_dbに加算）の初期値。聴感ミックス済みとは扱わない。
Web exportで診断音源を除外。ローカルZIPに16SEのimportがあり診断音源0件を確認。公開なし。
sound / generated_sound / preparation_ui / preparation_redesign / extension_boundaries は制限外Godot4.7.2でPASS。新テストは途中で上書きされた再生もweakrefで追跡しミキサー解放を待つ。初回の混在ID比較エラーをsame_entryへ修正して再検証。最終対象テストはエラーなし。
追加生成なし。音源ファイルの編集なし。実試聴・実プレイでの聴感品質は未確認。既存の生成manifest条件を保持。
