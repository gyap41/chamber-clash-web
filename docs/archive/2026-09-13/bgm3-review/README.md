# 始まりの工房 BGM3曲：素材レビュー

## 最新結果：チャージ後に3曲生成成功

ユーザーのチャージ完了と続行指示により残高を確認し、既存CLIで1コマンド1曲ずつ生成。3回すべて成功、追加生成なし。元WAVと生成条件・sha256をmanifestに保持。派生ループ編集版は作成していない。

|用途・元WAV|実長|ピーク|フルスケール到達サンプル数（左右合計）|先頭／末尾の低音量区間|
|---|---:|---:|---:|---|
|[タイトル／キャラ選択](../../../../assets/audio/bgm/fw_title_01.wav)|60秒|0 dBFS|5,651|2.40／0.58秒|
|[準備／インベントリ](../../../../assets/audio/bgm/fw_preparation_01.wav)|90秒|0 dBFS|3,223|1.36／1.23秒|
|[通常戦闘](../../../../assets/audio/bgm/fw_battle_01.wav)|120秒|0 dBFS|3,506|2.05／1.82秒|

全曲44.1kHz・ステレオ・16bit PCM WAV。低音量区間は10ms窓のRMSが-60 dBFS未満となる連続区間であり、完全無音の断定ではない。最長区間は各曲とも上表の先頭区間。全サンプルを走査し、PCMの上下限到達を検出。聴感上の歪み・既に焼き込まれたクリッピングは未確認で、単なる音量低下では元の歪みを復元できない。

Godot 4.7.2でインポート後、既存 `tests/audio_assets.gd` が全件成功。新規3曲をAudioStreamWAVとして60/90/120秒と認識。初回はユーザーデータディレクトリ権限エラーがあり、検証プロセス限定でAPPDATA/LOCALAPPDATAを `.local/godot-review` へ指定して完了。OS証明書ストア読み取り警告は残るが、ローカル音声Resource確認は成功した。

**採用候補は3曲とも要確認。完成ループとしての採用確定なし。** 音声を聴取できる機能がこの環境で確認できず、試聴回数0、loop_verified=falseを維持。曲調・声やSEの混入・3曲の統一感・射撃や警告との聴き分け・歪み・継ぎ目はユーザー確認が必要。先頭／末尾をそのまま反復すると間が空く可能性がある。小節の実位置が未確認のため、要求BPMだけに基づく切り出しや機械的クロスフェードは行わなかった。各曲を最低3周確認し、必要な小節位置でのローカル編集を判断する。追加API生成はしない。

残高は生成前約1,005→生成後約945 credits、観測差 **60 credits**。3成功×20 creditsの公式料金見積りと一致（換算$0.60）。個別の請求明細ではなく残高差での確認。詳細は [usage.json](usage.json)、[音声解析](audio-analysis.json)、[英語プロンプト](plan.json)。ゲームへのBGM接続・SE生成・画像生成は行っていない。

以下は生成前の停止・診断履歴。

## 後続の原因調査

ユーザーの原因確認依頼により診断。キーは既存 `.env` から読み込み成功（値は非表示）、既存TLS openerの構築も成功。通常の制限内実行では残高GETの接続時に `URLError` / `PermissionError`、errno 13、Windows socket error 10013を確認し、HTTP応答は取得できなかった。同じキー読込・TLS・User-Agent・残高endpointを維持し、承認された制限外実行でGETを1回行うとHTTP 200で成功した。この比較から、今回再現した接続失敗は制限内実行のソケット権限によるものと判断する。前回の汎用エラー表示だけではこの段階が分からなかった。

取得残高は **5 credits**。1曲20 credits、3曲60 creditsに不足するため、通信制限を解消しても現在の残高では生成できない。残高不足の停止条件に従って有料生成は0回のまま。チャージ・契約変更・設定変更・生成API送信なし。以下は初回停止時の記録。

2026-09-13。今回の承認はタイトル60秒・準備90秒・通常戦闘120秒を各1候補、最大3送信・60 credits。素材生成とレビューのみ。

## 結果

残高確認の読み取りGETでエラーとなり、ユーザー指定のエラー時停止方針に従って停止。有料生成POSTは0回、生成物なし。再試行・provider切替・チャージなし。残高は取得できず、原因は特定していない。秘密保護のため例外詳細・応答本文・認証情報は表示も保存もしていない。

無料のCLI dry-runは3曲とも成功。同名と生成条件fingerprintの重複が既存manifest・試行履歴にないことを確認。既存BGMの3試行（成否不明2、成功1）を保持し、履歴の削除やallow-repeatの使用はない。

## 制作条件

|用途|要求尺|名前|状態|
|---|---:|---|---|
|タイトル／キャラ選択|60秒|fw_title_01|未生成|
|準備／インベントリ|90秒|fw_preparation_01|未生成|
|通常戦闘|120秒|fw_battle_01|未生成|

英語プロンプト全文は [plan.json](plan.json)。各曲の `*.dry-run.json` に統一CLIの検証結果を保存。Stable Audio 2.5、既存endpoint・認証方式・WAV・steps 8・timeout 180秒を維持。

木の打楽器・控えめな撥弦・温かい低音・精密な電子音と、短い上行／応答のモチーフ方向を共通指定。独立生成で旋律が一致する保証はない。4/4と反復フレーズは要求であり、生成後の実測値やループ保証ではない。

## 料金

2026-09-13に [公式料金](https://platform.stability.ai/pricing) と [公式API仕様](https://platform.stability.ai/docs/api-reference) を確認。Stable Audio 2.5は1成功20 credits、1 credit=$0.01。3成功の見積り60 credits=$0.60。今回の生成送信は0回なので料金表による生成料金見積りは0 credits。実残高・実消費は取得できていない。

## 実装確認・未確認事項

AGENTS.md、共通設定ガイド、音響CLI README、AUDIO_BIBLE、音響素材数調査、manifest、始まりの工房コンセプトREADMEを参照。最新のtitle.gd、character_select.gd、preparation.gd、main.gd、combat_session.gd、sound.gdを確認。CPU選択から準備、90秒の通常対戦へ進み、準備操作・射撃・被弾等のSEが重なる。現在は生成SEの接続が進んでおり、旧資料の「保存済みSE1件／合成のみ」は現状の全体を表さない。今回はSEとゲーム再生処理を変更していない。

新規音源がないため再生リンク、実長、クリッピング、無音、Godot Resource認識、曲調、継ぎ目、3周試聴は未確認。元音源／派生ループ版ともなし。manifestは変更せず、loop_verifiedも変更しない。採用候補はまだなく、3曲とも残高確認後の生成と音声レビューが必要。

再開には残高確認エラーの原因調査と解消が必要。生成の再開時には最新の残高と試行履歴を再確認し、この承認上限を守る。
