# リナ基準の他7人への展開

2026-09-12。ユーザー「これを基準にほかのキャラクターへの展開を行いましょう。」に基づき、各1枚の方向別・専用回避シートを生成し、選択画像とゲームへ組み込んだ。

## 制作と加工

既存CLI・gpt-image-2・high・1024四方を維持。各キャラの採用済み低頭身設定とリナの方向別原画を参照。1枚に正面・背面・側面の3方向×立ち姿・踏み込み・活動・着地の4ポーズを配置。7枚を順次検証後に生成し、追加候補・再送なし。

元の種族・衣装を確認。ソラの生成側面は左向きだったため全側面ポーズを反転して右向き基準へ統一。コハクの尾、ルナとクロウの隣接行の張り出しが別コマへ混入する箇所は切り出し境界を調整した。

各方向の立ち姿の全高224pxを基準に倍率を決め、同じ方向の回避にも共通適用。靴の領域を明示し、再サンプル後の支持中心128±0.5px・底辺240pxへ平行移動。ゲーム表示は1/4。回避は512×384、接地原点256,320。元画像は上書きせず、全レシピを保存。

共通描画は `scripts/visuals/character_directions.gd`。リナの歩行オフセット・体重移動を再利用し、通常方向と回避移動方向の切替を全8人へ適用。旧7人用の立ち絵の回転・伸縮による回避処理を除去。性能・当たり判定・武器データは変更しない。

## 生成記録

今回7送信すべて成功、usage換算合計$0.7843515。全体45送信＝成功44・過去の成否不明1。成功分累計$4.910573。保守予約$45、予約上限$46、回数制限50を維持。成否不明の予約を解放せず、usageや課金有無を推測しない。金額は請求確定額ではない。

## 検証

Godotインポート、全ヘッドレステストに合格（`.local/logs/run_tests-20260912-210616.log`）。新設 `character_directions` は全7人の靴の支持範囲、専用ポーズ、4方向・武器前後関係、回避の移動方向優先、0.26秒・無敵0.31秒を確認。既存リナの0.38秒・153.4px検証も合格。CLI予算・異常時停止の6テストも合格。

`tools/capture_direction_rollout.gd` で全8人の待機・歩行・回避を4方向、128フレーム撮影。正面・背面・左右の実描画と切り出し結果を目視確認。GIFは比較用のその場アニメーションで、実ゲーム速度や人間による操作感の確認とは区別する。描線差・帽子等による顔の大きさの違いは残る。

[現行の素材と映像](../../../art/reviews/character-directions-2026-09-12/README.md)。通常等身を含む旧資料は `CHARACTER_BIBLE_BEFORE.md` に保存し、現行 `CHARACTER_BIBLE.md` は採用した種族・衣装・描画基準へ整理した。
