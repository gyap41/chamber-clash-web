# リナ：体型と独立パーツの試作

## 決定事項（2026-09-26、ユーザー）と本編への仮組み込み

- デザイン：リュックなし。スカーフの垂れは残し、色は橙色。弾みは「強」。
- 方向：見た目も8方向。描き分けは正面・斜め前・側面・斜め後ろ・背面の5向き（左向き3方向は反転）。
- 順序：残り4向きの生成より先に、側面v7を本編へ仮組み込みして確認する。

側面の仮組み込み（当時の `scripts/visuals/rina_side_run.gd`。v8で `rina_run_rig.gd` に置き換え、旧スクリプト・素材・書き出し手順は削除）（既定無効、ゲーム中 V キー（F8 はエディターの実行停止と重なるため不可）または起動引数 `-- --rina-side-run` で切替）：左右向きの待機・歩行だけを v7（強）に置き換え、正面・背面・回避・他キャラは従来の素材のまま。銃は本編 Weapon の変換（照準角・反動・装填の傾き）を使い、grip を手の位置へ移す。素材は `export_rina_side_run.py` で `assets/first-workshop/rina-side-run/`（body.png・head.png・rig.json）へ書き出す。

確認結果（技術検証。美術品質・ユーザー採用ではない）：
- 自動テスト：当時の `tests/rina_side_run.gd`（v8で `tests/rina_run_rig.gd` に置き換え）合格（既定無効、側面だけ置換、grip と手の一致、8コマ巡回と後退の逆順、正面・背面・回避・他キャラは従来、切替で復帰）。既存の関連10テスト（rina_directions・character_animation・character_motion・characters・actor_animation_state・character_directions・equipment_art・player_animation・weapon_readability・dodge_flow）もアサーション失敗・スクリプトエラーなし。終了時の「resources still in use」は変更前から出ている。
- 実描画（Godotで撮影、一時画像は `.local/rina-v6/cap-*.png`）：照準 ±26° で銃が手を中心に回り、両腕が追従。左向きは反転。銃は頭の後ろを通り顔は隠れない。武器00・19・04で確認。
- 足滑り：本編の歩行位相（約0.43秒/周期＝8コマで約18.7fps）では前進の見かけが約55px/秒、リナの移動速度は205px/秒で約3.7倍の差。本編で足が滑って見える可能性が高い。
- 既知の点：手袋の輪郭が細くぼやける、読みやすさ用の輪郭シェーダーは描いた銃に未適用、装填時の上下の揺れ（-3px）は手の位置合わせで打ち消される、側面と正面・背面の切替で絵柄（旧素材はリュックあり）が変わる。
- ユーザー確認（2026-09-26）：本編で V キーにより確認し「そんなに違和感は感じなかった」。足滑り・後退・腕の見え方を含め、側面の仮組み込みは許容範囲との評価（正式採用ではない）。これを受けて残り4向きの生成へ進む。

残り4向きの生成（2026-09-26、ユーザー依頼によりClaudeが実行、各1回、high、1024×1024）：
- `assets/generated/rina-front-back-parts-v1.png`（推定0.115ドル）：正面の頭＋胴・ポニーテール、背面の頭＋胴・ポニーテール・スカーフの垂れ。指示全文 [views-front-back-prompt.txt](views-front-back-prompt.txt)。
- `assets/generated/rina-diagonal-parts-v1.png`（推定0.115ドル）：斜め前・斜め後ろの頭＋胴・ポニーテール・スカーフの垂れ。指示全文 [views-diagonal-prompt.txt](views-diagonal-prompt.txt)。
- 参照はターンアラウンド全体（1536幅）と側面の生成パーツ（作成手順 `build_views_refs.py`、出力は `.local/rina-v6/`）。画像CLIの使用は81/82件（残り1件）。
- Claudeの目視評価（ユーザー未確認）：全パーツが分離し、腕・脚・銃・リュックなし。絵柄・配色・線の太さは側面と一致。頭＋胴は側面の生成より約25%小さく描かれたため、組立時に髪留め・目などの基準点で倍率を合わせる。背面の胴にはスカーフの三角の端が描き込まれている（別パーツの垂れと重ねる）。斜めのポニーテールとスカーフの向きは組立時に確認・必要なら反転する。

## 走行 v8：8方向（描き分け5向き）の組立と本編への仮組み込み（最新・未採用）

### 複数キャラ向けの一般化と近接（ナイフ）（2026-09-26）

ユーザー合意「キャラごとに用意するのは止まった絵のパーツと設定ファイルだけ、動きは共通のコード」に沿って整理。
- ビルダー：`build_run_cycle_v8.py` のリナ専用の値を設定ファイル `tools/character_rig/characters/rina.json` へ移し、共通ビルダー `tools/character_rig/rig_builder.py`（実行は `build.py rina`）にした。部品は `pick` の点に最も近い連結成分で取り出す。書き出し結果（画像42枚）は整理前と画素単位で一致を確認。`build_run_cycle_v8.py`・`build_arm_parts.py` は経緯の記録として残す（現行の書き出しは共通ビルダー）。
- ゲーム側：`rina_run_rig.gd` → `scripts/visuals/character_rig8.gd`。`data/character_rigs.json` の登録キャラを対象に、キャラごとに素材を読み込む。揺れ物の揺れ方は種類（hair/cloth）で決まる。
- 作業支援：基準点をクリックで設定する `tools/character_rig/landmark_picker.html`、生成指示の型 `tools/character_rig/templates/`、手順書 `tools/character_rig/README.md`。
- 近接（ナイフ）：`assets/generated/rina-knife-v1.png`（gpt-image-2、high、1回、推定0.114ドル、使用84/86件。指示の型 `templates/melee_weapon.txt`）。体は4コマ（踏み込み→振り抜き→戻り→構え、0.26秒）、奥の手でナイフ（全長21ゲーム画素）を照準の -60°→+60° へ振り、銃は手前の手に持ったまま。照準方向へ最大7pxの見た目の踏み込み、斬撃の弧は当たり判定と同じ扇形。
- 近接の当たり判定（全キャラ・CPU共通、ユーザー了承「攻撃範囲は見た目に寄って変えてよい」）：角度±60°→±70°。距離は一度64→52pxにしたが、ユーザー確認で「短い」との評価を受け64pxへ戻した（踏み込みも5→7px）。CPU の近接判断は近接の射程の値を参照。
- 確認（技術検証）：`tests/character_rig8.gd` と近接に関わる既存テストを実行。`equipment`（武器スロット切替、58行目）と `exploration_rooms`（探索カメラ倍率が1でない、106行目）が失敗するが、前者は近接の変更を一時的に戻しても同じく失敗し、後者は作業前からある探索カメラ倍率1.2の変更によるもので、今回の変更が原因ではない。

2026-09-26、Claudeが制作。ユーザー合意「生成は向きごとの止まったパーツ、動きはコード。脚は制作時に1回だけ3次元の軌跡から投影して下書きし、ゲーム中はコマ再生（後退は逆再生、横移動専用の足運びは作らない）」に沿う。弾みは「強」のみ。

- 組立（`build_run_cycle_v8.py`）：向きごとの設定表 `VIEWS` に、生成画像上の髪留め・裾・首の切り分け線・肩・手・ポニーテール/スカーフの付け根（目視で読んだ座標）、重なり順、腕と銃の置き方を記録。倍率は髪留め〜裾の長さを側面にそろえる（正面0.585・背面0.588・斜め0.697、側面0.44）。斜め後ろのポニーテールとスカーフは前方向へ流れて生成されたため左右反転して使用。背面は胴に描き込まれたスカーフの三角の端を使い、別パーツの垂れ（B3）は未使用。
- 脚：v6の側面の脚表を3次元の足の軌跡とみなし、向き（側面0°・斜め前45°・正面90°・斜め後ろ-45°・背面-90°）ごとに投影。膝は脚の前後面内で曲げ、靴は側面の輪郭に左右の厚みを付けて投影（正面・背面は少し幅広）。奥行きは画面の下方向へ小さく写す。側面は v7 と同じ見た目になるよう、腰の前後のずれ（±7）を保つ。
- 上体：弾み・縮みは全向き共通、前傾は側面成分（cos）だけ。ポニーテールとスカーフの揺れ角は v7 と共通で、正面・背面は0.5倍、斜めは0.85倍。
- 動作の追加（2026-09-26、ユーザー指摘「待機動作がない」「回避中のモーションも必要」）：
  - 待機：両足接地の立ち姿、2.6秒周期（本編の待機と同じ）の呼吸8コマ。上体を上下・伸縮し、ポニーテールとスカーフは遅れてゆっくり揺れる。銃と腕は通常どおり。
  - 回避：6コマ（踏み込み→踏み切り→飛び込み→着地前→着地→立ち直り）。0.04/0.10/0.20/0.26/0.32秒で切り替え、0.04〜0.26秒は旧回避と同じく最大6px浮く。体を進行方向へ倒し（側面で最大72°、斜めは0.71倍）、正面・背面では倒れ込みを縦の潰れと沈み込みで表す。脚は後ろへ伸ばし、着地で縮む。銃が隠れている間（操作制限0.31秒）は両腕を前へ伸ばし、立ち直りのコマで銃を再表示。向きは移動方向で選ぶ。
  - 回避で体を倒すと頭が前へ出るため、セルを右へ80画素広げた（1コマ 261×241）。
- ポニーテールの位置（2026-09-26、ユーザー指摘「位置が少し高い」）：比較 [ponytail-compare.png](ponytail-compare.png)（現状・A 付け根を後頭部寄りに下げる・B 後ろへ倒す・C 両方）からユーザーが C を選択。頭頂の髪留めは消し、箱の左右の頭の輪郭から放物線で輪郭を補う（`erase_ring()`）。付け根の移動は側面(-12,+14)・斜め前(-9,+14)・正面(0,+16)・斜め後ろ(-8,+14)・背面(0,+22)セル画素、倒す角は側面-22°・斜め-18°、正面・背面は縦を0.78倍（`PONY_STYLES['C']`）。
- 髪・スカーフの揺れ（同日、ユーザー要望「待機や移動中に揺れる表現」）：ポニーテールとスカーフの垂れを体のシートから外し、向きごとの別画像（付け根・毛先方向・長さ付き）として書き出す。ゲーム側（`rina_run_rig.gd` の `update_swing` / `draw_part`）が毎フレームの減衰ばねで角度を求め、6×6の格子に分けて付け根から毛先へ曲がりが増えるように描く。入力は コマごとの基準角（従来の焼き込み角）・前進の速さのなびき・付け根の加速度（体の上下、動き始め/止まり、回避）・ゆるやかな揺れ（約2.6秒周期）。スカーフは速さに応じてはためく波を重ねる。向き・反転の切替直後は加速度の履歴を捨てる。後ろ髪の房（側面）は従来どおりシート内のワープ。
  - 揺れ幅（Godotで60fps計算、角度の上下幅→56px表示の先端の動き）：走行 ポニーテール約15°（約3.7px）・スカーフ約15°（約3.1px）＋はためき、待機 ポニーテール約15°（止まった直後の揺り返しを含む）・スカーフ約9°＋はためき、回避 どちらも40°台。確認GIF [run-cycle-v8-swing.gif](run-cycle-v8-swing.gif)（走る→止まる→回避、左から 側面・斜め前・正面・斜め後ろ・背面、描画位置だけ固定し揺れの入力は実際の移動量）。
- 参考動画との比較による修正（2026-09-26、ユーザー指摘「正面の両足がくっついている」とレビュー。比較は `.local/rina-v6/legs-compare.png`、参考動画の切り出しは私的参考のためリポジトリに置かない）：
  - 正面・背面の足：足首を腰より外へ出して裾の左右の端から足が出る「ハの字」にし、つま先を外へ18°向ける。持ち上げは35%控えめにし、上げた足は少し外へ振る（`FOOT_OUT`・`SWING_OUT`・`TOE_OUT`・`LIFT_FB`、いずれも |sin yaw| で効かせ側面は不変）。
  - 待機：足を前後に開いて左右非対称にし、側面の膝の曲げを浅く。
  - 銃：背面・斜め後ろは手を顔の高さへ上げ、銃が頭の上・肩の横に見えるように（参考動画の背面に合わせる）。正面は手を左へ寄せて銃を体の中央へ。
  - 背面：未使用だったスカーフの垂れ（B3）を首の後ろに付けて揺らす。正面・背面の揺れの抑えを 0.5→0.8（パーツの基準角）、0.45→0.75（ばねの左右成分）に弱めた。
  - 正面・背面の回避：縦に潰す代わりに体を横へ傾ける（飛び込み時に最大26°）。
  - 参考動画との再比較（同日）で追加修正：正面・背面で真下/真上を狙うとき、見た目の銃だけ最大30°右へ開いて体の前で斜めに構える（`visual_angle()`、弾の向きは不変）。正面の手を胸の高さへ。正面・背面の回避の横傾きを0.5→0.2倍。斜めでも足を外へ開く（重み |sin yaw|×1.4、上限1）。
  - 画像CLIの上限はユーザー依頼により 82→86（2026-09-26）。`rina-arm-parts-v1` は操作の取りやめ時に送信済みだった（台帳 pending・ロック残存、画像未受領）。ユーザーが OpenAI の Usage で当日(UTC)3件を確認し課金済みと判断、「進めてください」を受けて台帳を outcome_unknown（予約保持、再実行名 v2）に解決しロックを削除。
  - 腕・手袋（2026-09-26）：`assets/generated/rina-arm-parts-v2.png`（gpt-image-2、high、1回、推定0.115ドル、使用83/86件。指示全文は `.local/rina-v6/rina-arm-parts-v1-prompt.txt` と同名JSON）。袖2種・握り4種（横・正面・手の甲・斜め）・支える手・前へ伸ばす手（横・正面）。`build_arm_parts.py` が切り出し（手袋の幅7ゲーム画素）、ゲーム側は袖を肩→肘→手首に沿って伸ばし、手袋を銃の向き（握り）または前腕の向き（伸ばす手）へ回して描く。向きごとに手袋を選び、長い武器の側面・斜め前は前の手を「支える手」に。背面・斜め後ろは肘を外へ張り、上腕の袖だけを体の上に重ねて肩の上に袖が見えるようにした（手は頭の陰）。コードの筒と丸の腕は廃止。
  - 腕は形・絵柄とも未解決（コードの筒と丸）。次に腕・手袋の生成パーツへ置き換える予定（ユーザー合意済み、生成枠の引き上げ待ち）。
- 参考動画との再比較による修正（2026-09-26、腕の生成パーツ導入後）：背面は上げた腕の上腕の袖を肩から外・上へ（約50°、上腕長の1.4倍）体の上に重ね、頭の上で構える形に。斜め後ろは手を右肩の外・肩の高さへ出し、重なり順を側面と同じ（遠側の腕→体→銃→手前の腕）にして銃と腕が見えるように。手袋の幅を7→5.5ゲーム画素に。正面で真下を狙うときの銃の開き角は、長い武器だけ30°→18°（銃床が頭の横まで上がるため、`is_long_gun()`）。回避の煙の演出は別作業として未着手。
- キャラの大きさ（2026-09-26）：参考動画のキャラは画面高の約13%（約95〜105px／782px）、現行の探索画面は約8%（約51〜53ゲーム画素×カメラ1.2／800px）。ユーザー選択の案D（カメラを寄せ、キャラも少し大きく）を試験用に実装（C キー、既定無効）。詳細は ACTOR_ANIMATION.md。
- 出力：`assets/first-workshop/rina-run-8dir/`（5向き×3動作×下層/上層のシート、向きごとのポニーテール・スカーフ画像、`rig.json`）、確認用 [走行](run-cycle-v8-run-views.png)・[待機](run-cycle-v8-idle-views.png)・[回避](run-cycle-v8-dodge-views.png)（銃・腕なし、行＝向き）。
- 本編：`scripts/visuals/rina_run_rig.gd`（既定無効、V キーまたは起動引数 `-- --rina-run`）。待機・走行は照準角、回避は移動方向で8方向（45°刻み、境界付近は前の向きを22.5°+8°まで保つ）。重なり順は 側面・斜め前：遠側の腕 → 体 → 銃 → 頭 → 手、正面：体 → 銃 → 頭 → 両腕、斜め後ろ・背面：銃と両腕 → 体。他キャラは従来。

確認結果（技術検証。美術品質・ユーザー採用ではない）：
- 自動テスト：新規 `tests/rina_run_rig.gd` 合格（8方向の向きと反転、境界の保持、各向きで grip と手が一致、走行の巡回・後退の逆順、待機・回避のコマ送りと浮き、回避は移動方向・銃を隠し両腕を前へ、他キャラは従来、V キー切替）。既存の関連12テスト（旧回避の rina_dodge_poses・rina_dive を含む）もアサーション失敗・スクリプトエラーなし。
- 実描画（Godotで8方向×拳銃00・武器19、8コマ撮影。一時画像 `.local/rina-v6/dir8-f*.png`）：全方向で腕・銃・体の重なりが破綻なし。初回撮影で左向き3方向の遠側の腕が背中側へ出る不具合（呼出し側の向きの変換が残っていた）を見つけて修正済み。
- 待機・回避の実描画（Godotで8方向を撮影、一時画像 `.local/rina-v6/dodge-t*.png`・`idle-t*.png`）：回避6段階と待機が全方向で破綻なく表示され、回避の軌跡（従来の演出）も残る。正面・背面の回避は倒れ込みが縦の潰れだけなので、側面・斜めより飛び込み感が弱い。
- 既知の点：正面・背面の脚は細めで靴が小さい。正面では銃が下向きに体の前へ伸び脚に重なる（本編の銃の大きさ・向きの規則どおり）。背面では銃がほぼ隠れる。側面と斜めの切替で頭の大きさがわずかに変わる（倍率は髪留め〜裾で合わせている）。（回避も新しい描画に置き換え済み。旧回避の素材は仮組み込みが無効のときに使われる。）

## 側面走行サイクル v7：生成した胴体パーツ＋本編の銃＋コード描画の両腕、銃は頭の後ろ（最新・未採用）

2026-09-26、Claudeが制作。v6の仮胴体を、合意方針6の生成パーツに差し替えた。弾みはユーザー指定の「強」をレビューの既定にし、「中」も切替で残す。

- 生成：`assets/generated/rina-side-body-parts-v1.png`（gpt-image-2、high、1024×1024、1回、推定0.113ドル。生成条件は同名JSON）。白背景に A=頭＋胴（腕・銃・脚・ポニーテール・スカーフの垂れなし）、B=ポニーテール、C=スカーフの垂れ の3パーツ。指示全文は [run-body-parts-prompt.txt](run-body-parts-prompt.txt)。参照はターンアラウンドの右側面と、v4上体から銃と手を消したものの2枚（作成手順 `build_body_parts_refs.py`、出力先は `.local/rina-v6/`）。画像CLIの上限はユーザーが79→82件（+3件）に変更し、この1件で使用79件。
- 生成結果の評価（Claudeの目視）：3パーツとも分離し、腕・銃・脚は含まれない。顔・ゴーグル・髪色・服の配色はターンアラウンドと一致。胴はv4原画より小さく、肩の丸い膨らみが描かれている。ポニーテールには頭側と同じ金の髪留めが付く。ユーザー未確認。
- 組立（`build_run_cycle_v7.py`）：外周とつながる白を透明化し輪郭の白混じりを分離、連結成分で3パーツへ分ける。倍率0.44で頭頂〜裾の高さをv4に合わせ、裾の中央を腰の少し前に置く。ポニーテールは髪留めを頭側の髪留めに重ね、スカーフの垂れは付け根を首の後ろに付けて胴の後ろに置く。どちらも付け根まわりに減衰ばねの角度で回す（切り抜きではないので切れ目なし）。後ろ髪の房だけワープで揺らす。脚・弾み・ばねは v6 と同じ。ポニーテールが揺れても切れないよう、コマは上に余白を足した 221×241。
- 描画順：銃を「胴の前・頭の後ろ」に描く（遠側の腕 → 体 → 銃 → 頭 → 遠側の手 → 近側の腕）。本編の銃の大きさのままでも顔が隠れず、銃は頬の後ろを通る。頭は首の線より上を別シートに出力。レビューのチェックを外すと本編と同じ「頭の上に銃」と比較できる。
- 腕：両腕とも肩→肘→手の2関節をコードで描く（上腕・前腕とも23シート画素）。袖は生成胴の生成り色、手袋は茶。

56px換算（`build_run_cycle_v7_review.py` が計測、拳銃00。表示倍率はコマ0の最上点〜床＝56px）：

|項目|強（既定）|中|
|---|---|---|
|頭（ゴーグル中心）の上下幅|3.33px|2.60px|
|輪郭の最上点（ポニーテール込み）の上下幅|4.00px|3.20px|
|両足空中|4/8コマ|4/8コマ|
|滞空の足の持上げ最大|1.60px|1.60px|
|銃口の上下幅|0.61px|0.59px|

ファイル：[レビュー](run-cycle-v7-review.html)（弾み強/中、武器38種、描画順の切替、銃と腕の表示切替、56px・カメラ追従・固定・×4・コマ送り・速度・低速・左右反転・前後コマ重ね・接地表・計測値）、確認GIF（[強](run-cycle-v7-strong.gif)／[中](run-cycle-v7-medium.gif)、上段拳銃00・下段武器19、3周期）、`run-cycle-v7-<強中>-body.png`（後ろ髪・スカーフ・脚・胴＋頭）、`run-cycle-v7-<強中>-head.png`（頭のみ）、`run-cycle-v7-meta.json`。再現は `python build_run_cycle_v7.py` → `python build_run_cycle_v7_review.py`（このフォルダーで実行、Pillow/numpy/scipy が必要）。

検証：レビューHTMLをローカルHTTPで開き、読込・弾み切替・武器切替（00・19・04・20・33）・描画順切替・コマ送りでコンソールエラーなし。拳銃00（両方の描画順）・武器19・04の合成コマを拡大で目視。技術検証であり、美術品質やユーザー採用の合格ではない。

未対応・判断が必要な点：
- 長い武器の床尾は腰〜脚の前にかかる（参考動画のライフルも同様）。前の手は短い腕が届く範囲までしか伸びない。
- 描画順「銃を頭の後ろに」を本編にも入れるかは未決定（本編は現在、銃が頭の上）。
- 頭シートの下端（首の線）に襟の色が数画素含まれる。銃が頭の後ろを通るとき、顎の下で襟の一部が銃の上に出る。
- 武器20は grip 未定義のため握り位置は近似。武器33など grip が中央にある特殊な形は、構えの見え方を個別に確認していない。
- 左右・後退・正面/背面、射撃・回避、Godot本編への接続は未実施。

## 側面走行サイクル v6：本編の銃画像＋コード描画の腕、ワープによる髪・スカーフの揺れ、弾み「中」「強」の作り直し（仮胴体・未採用）

2026-09-25、Claudeが制作。v5レビュー（同日、下記「v5レビューの指摘」）を受けた修正版。合意方針（[引き継ぎメモ](../../../notes/2026-09-25-rina-motion-handoff.md)の「v5で合意した方針」）の1〜5に対応し、6（胴体素材の生成）は未実施。

- 銃：1枚絵から切り抜かず、`assets/first-workshop/equipment/guns/*.png` を `data/weapon_visuals.json` の body.grip・muzzle・scale で配置する。大きさは本編 `player.gd update_weapon_art()` と同じ規則（40×30のゲーム画素枠に収める倍率×scale）。grip を近側の手に合わせ、照準角0°を保つ。レビューHTMLで38種を切替可能。武器20は grip 未定義（fixed_width方式）のため握り位置は近似。
- 腕：コードで描画。遠側は肩→支持点を2関節（拳銃は握りの下、長い武器は銃身の下。腕が届く範囲まで）。近側は仮胴体の袖口→grip の手袋。描画順は遠側の腕→体→銃→遠側の手→近側の手袋。
- 胴体は仮：v4の上体原画から銃・手袋・撃鉄を多角形で消し、切り口に輪郭を付けたもの。近側の上腕〜前腕の袖は原画のまま残るため、近側は肩からではなく袖口から描いている。
- 髪・ポニーテール・スカーフ：切り抜き・穴開けをやめ、上体画像全体を1回のワープで変形（根本の重み0→毛先1のsmoothstep、前傾・縮み・上下も同じ写像）。v5の切れ目・顎の移動・スカーフの二重輪郭は構造上出ない。揺れ角は頭の上下加速度を入力にした減衰ばね（部位ごとに固有振動数・減衰・常時のなびき角が異なる）。
- 弾み：半周期4コマ＝着地（縮み・最下点）→蹴り出し（伸び）→滞空↑→滞空↓。両足空中は8コマ中4コマ（v4・v5は2コマ）。脚表を作り直し、前後に大きく開き、後ろ足を高く跳ね上げる。前傾は中5°・強8°を基準に着地で深くなる。
- 銃の追従：体の上下へ1コマ遅れ、中40%・強35%だけ追従。腕の長さがその差を吸収する。
- レビューページ：弾み中/強、武器38種、銃と腕の表示切替、56px（直立の全高基準で両案同じ倍率）・カメラ追従の床スクロール・カメラ固定の横断・×4拡大・コマ送り・速度/低速・左右反転・前後コマ重ね・接地表・各コマの上下/前傾/縮み/揺れ角、56px換算の計測値。

56px換算の数値（build_run_cycle_v6_review.py が計測、拳銃00）：

|項目|中|強|v4/v5中（参考）|
|---|---|---|---|
|頭（ゴーグル中心）の上下幅|2.59px|3.32px|1.61px（頭頂）|
|輪郭の最上点（ポニーテールの揺れ込み）の上下幅|2.46px|3.55px|—|
|両足空中|4/8コマ|4/8コマ|2/8コマ|
|滞空の足の持上げ最大|1.64px|1.64px|0.54px|
|銃口の上下幅|0.60px|0.62px|v5強 2.21px|

ファイル：[レビュー](run-cycle-v6-review.html)、確認GIF（[中](run-cycle-v6-medium.gif)／[強](run-cycle-v6-strong.gif)、上段拳銃00・下段武器19、3周期）、`run-cycle-v6-medium-sheet.png`／`run-cycle-v6-strong-sheet.png`（体＋脚のみ、各8コマ、1コマ221px、RGBA）、`run-cycle-v6-meta.json`（各コマの手・袖口・遠側の肩、接地、足送り11px/コマ、表示基準の全高）。再現は `python build_run_cycle_v6.py` → `python build_run_cycle_v6_review.py`（このフォルダーで実行、Pillow/numpy/scipy が必要）。

検証：レビューHTMLをローカルHTTPで開き、読込（コンソールエラーなし）、弾み切替、武器切替（00・04・19）、コマ送り、×4拡大を確認。拳銃00・武器04・19で合成した8コマと56px GIFを目視。これは技術検証であり、美術品質やユーザー採用の合格ではない。

未対応・判断が必要な点：
- 胴体素材の生成（方針6）：`tools/generate_image.py --check` が「Conservative budget would reach the authorized limit.」で停止（使用量台帳78件、上限 `LIMIT_REQUESTS=79`）。API送信なし。→ 2026-09-26、ユーザーが上限を変更し生成。v7で差し替え済み。
- 本編の銃の大きさのまま持たせると、拳銃00など背の高い画像は顔の下半分を隠し、長い武器は床尾が腰〜脚の前に来る。本編の表示規則を変えるか、走行時だけ手の位置を上げるかは未決定。
- 長い武器の前の手は、短い腕が届く範囲（近側の手から約4ゲーム画素前）までしか伸ばせない。
- 左右・後退・正面/背面、射撃・回避、Godot本編への接続は未実施。

## v5レビューの指摘（2026-09-25）

v5は合意方針の1〜4を満たしていなかった。武器は1枚絵の切り抜き（手＋拳銃＋顎の一部）で、本編の銃画像・grip は未使用、腕のコード描画なし。「中」はv4と同値、「強」も脚は同じで、56px換算の頭頂の上下幅は中1.61px・強2.65px、滞空の足の持上げ0.53px、両足空中2/8コマ。揺れは跳ね毛とスカーフが同じ値の列で2コマごとに反転し、切り抜き境界に明るい線・二重輪郭が出ていた。v5ページは床スクロール・足送りがなかった。v4の `run-cycle-sheet.png`／`run-cycle-meta.json` は18:56に再書込みされていたが、`build_run_cycle.py` の再実行結果およびv4レビューHTML内蔵画像と画素単位で一致（上書きによる変更なし）。

## 側面走行サイクル v5：体と武器の分離、髪・スカーフの遅延揺れ、弾み「中」「強」2案（不採用：方針未達、v6で作り直し）

2026-09-25、Claudeが引継ぎで制作。v4（ユーザー評価済み・未採用。「大分よくなった」が参考動画より弾みが足りない）を土台に、合意方針に沿って作る予定だったが、実際には下記の切り抜き方式にとどまった。新たな画像生成は行っていない。

- 方式：v4の上体固定素材（`run-keyposes-fix.png` セル2、頭・胴・腕・銃を含む1枚絵）から、武器（手＋拳銃、顎・口元の一部を含む）・頭頂の跳ね毛・後方の髪房・スカーフの4パーツを多角形マスクで切出し、胴体側にはその穴を開けた（`split_pieces()`、根拠は目視トレースした近似多角形。色によるセグメンテーションは試したが胴体側の指・襟outlineまで誤って巻き込み不採用）。
- 変換：各パーツはまず胴体と全く同じ変換（腰点中心の前傾＋上下）を継承し、そのうえで自パーツの根本点まわりに小さな遅延角を追加する2段階のPIL回転。胴体と同じ変換を先に揃えることで、パーツ境界の隙間（旧試作で発生した白い亀裂）を解消した。穴より大きく張り出させて切出す余裕（4〜9px）も別途確保。
- 遅延揺れ：`sway(f) = amp * (bob[f-lag] - bob[f]) / 10`。武器は振れ角最小（重さの示唆）、髪2箇所とスカーフはやや大きい振れ幅、遅延は1〜3コマ。
- 弾み「中」「強」：胴体の上下動・前傾角を2セット用意（中＝v4と同値、強＝上下・前傾とも約1.6倍）。遅延揺れの振幅係数は強で2倍（上下差の拡大と合わせ揺れ角は最大約3.2倍）。脚の形はv4と同一。レビューHTMLの切替ボタンで両案を見比べられる。

ファイル：[レビュー](run-cycle-v5-review.html)（弾み中/強の切替、56px/×4拡大、コマ送り、速度切替、低速、左右反転、前後コマ重ね、各パーツ揺れ角の一覧表）、確認GIF（[中](run-cycle-v5-medium.gif)／[強](run-cycle-v5-strong.gif)）、`run-cycle-v5-medium-sheet.png`／`run-cycle-v5-strong-sheet.png`（各8コマ横並び、1コマ221px、RGBA）、`run-cycle-v5-meta.json`（両案のフレーム情報・各パーツ根本点）。再現は `python build_run_cycle_v5.py` → `python build_run_cycle_v5_review.py`（このフォルダーで実行）。実行にはPillow/numpy/scipyが必要で、今回は `.local/video-deps` へ `pip install --target` で追加した（追加のAPI設定・課金なし、pip経由のローカルパッケージ取得のみ）。

検証：ヘッドレスChromiumでレビューHTMLの読込・コマ送り・弾み切替を確認（コンソールエラーなし、スクリーンショットで通常サイズ・拡大・全8コマ・揺れ角表の表示を確認）。パーツ境界に隙間が出ないことをコマ0・1・3・5の拡大画像で目視確認。これは技術検証であり、美術品質やユーザー採用の合格ではない。

未対応・次の確認点（当時の記載。v4は実際にはユーザー評価済み）：武器・髪・スカーフの切出し境界は目視トレースの近似（多角形の頂点は手動推定）であり、原画そのものを再構成したものではない。弾み「中」「強」のどちらが自然に見えるか、遅延揺れの振幅・タイミングが強すぎ／弱すぎないかはユーザー判断待ち。左右・後退・正面/背面、射撃・回避、Godot本編への接続は未実施。

## 側面走行サイクル v4：上体固定＋手描き脚（未採用）

ユーザー評価（2026-09-25）：「大分よくなった」。ただし参考動画に比べて弾みが足りない。歩幅を実際の移動量と正確に合わせる必要はなく、もっとダイナミックでよい。上下動（原画セルで最大7px）と滞空の持上げ（5px）は56px表示で約1px以下。

2026-09-25、Claudeが引継ぎで制作。新たな画像生成・有料APIの使用なし。8コマ一括生成の繰り返しをやめ、脚の接地交代を設計どおりに描ける方式へ変更した。

- 上体：`run-keyposes-fix.png` の3コマ目（横4×縦2の等分セル index=2）から裾より下を除去し、裾の輪郭線を補った。頭・髪・銃・腕は全コマ同一画像なので、コマ間の形状揺れはない。
- 脚：近側（明るい茶）と遠側（暗い茶）を別々にベクター描画。腿と脛は同色の連続形状で膝に継ぎ目を出さない。2関節IK（腿42・脛40、膝は常に前）、靴口の生成り色の帯、丸い靴と靴底。
- 周期：8コマ、既定16fps（0.50秒/周期）。コマ間の補間はしない。近側は0〜2コマ、遠側は4〜6コマで接地し、3・7コマは両足空中（滞空）。支持脚の交代を表で確認できる。
- 重心：上下（+下）0,7,2,−6（×2）、前傾1.5〜2.5°を上体に適用。接地コマは靴底を床線に一致、支持脚の足送りはシート画素で10px/コマに統一し、背景もコマ単位で同量だけ進める（足滑りなし）。

参考動画 8.3〜9.3秒（30fps直接デコード）との比較：参考も銃を構えた上体はほぼ固定し、短い脚を前後に大きく開く姿勢と、脚をそろえる姿勢が交互に出る。旧パーツ方式は脚画像の伸縮で交代が読み取りづらく、最新8姿勢は近側脚が常に前だった。v4は近側・遠側の前後関係が半周期ごとに入れ替わる。録画には重複フレーム（取り込み時の欠落）があるため、参考のコマ数・ホールドは断定しない。

ファイル：[レビュー](run-cycle-review.html)（56px追従/固定カメラ、×4拡大、コマ送り、速度切替、低速、左右反転、前後コマ重ね、接地表）、[確認GIF](run-cycle-v4.gif)、`run-cycle-sheet.png`（8コマ横並び、1コマ221px、RGBA）、`run-cycle-meta.json`（原点・床・足送り・各コマの接地）。再現は `python build_run_cycle.py` → `python build_run_cycle_review.py`（このフォルダーで実行）。

検証：ヘッドレスChromiumで読込・コマ送り・接地表示を確認（コンソールエラーなし）。接地脚の交代・床一致・足送りは数値で確認。これは美術品質やユーザー採用の合格ではない。

未対応・次の確認点：上体が1枚絵なので、髪・スカーフ・ポニーテールの追従（揺れ）がない。前傾・沈み込みの量、脚の太さ・靴の大きさが原画と揃っているかのユーザー判断。左右・後退・正面/背面、射撃・回避、Godot本編への接続は未実施。側面の一周期がユーザー採用されるまで全方向へ展開しない。

## 全身走行キーポーズの再生成（不合格）

ユーザーの生成許可に基づき内蔵imagegenを2回使用。三方向原画 `turnaround.png` を参照して8姿勢を生成し、次に脚の役割の入替を修正指示した。原本 `run-keyposes.png` と `run-keyposes-fix.png` を保存。指示全文は `run-keyposes-prompt.txt` と `run-keyposes-fix-prompt.txt`。初回は1774×887 RGBAで、指定2048×1024とは異なる。背景は指定のマゼンタではなく透過で返ったため、元のalphaを維持した。

[初回の連続再生](run-keyposes-review.html)／[修正生成の連続再生](run-keyposes-fix-review.html)。`build_keyposes_review.py` で再現。横4×縦2の等分セル、共通倍率、同一セル原点、周期0.64秒の仮再生。拡大と約56px表示、一時停止、コマ送り、低速、左右反転を備える。フレーム別の足元合わせ・拡縮・追加sin上下動は適用しない。生成素材の位置ずれも含めてレビューする。

美術レビュー：全身の沈み込みと前傾は前のパーツ方式より明瞭。ただし近側の脚を前へ出す形が両半周期で反復し、左右脚の接地交代と蹴り出しが成立しない。修正生成でも解消せず、不合格。髪・銃の微妙な形状変化も残る。単なる再生速度調整では解決できない。8コマ一括生成で正しい周期を得られたとは扱わず、対になる接地姿勢を個別に確定する工程が必要。本編組込み・全方向展開・ユーザー採用は未実施。

検証：初回プレビューのブラウザー表示と停止操作を確認。素材レビューと機能確認を、自然な連続動作の合格と混同しない。

## 参考動画を反映したモーション（旧パーツ方式・機械的とのユーザー評価）

### 足の慌ただしさを修正

ユーザー評価「カシャカシャ素早く動き、ロボットのよう」に対応。周期0.36→0.52秒、接地率44→50%、足上げ3.6→1.8px、胴体上下片振幅0.9→0.65px。足の戻しは5次補間、持上げはsin²で接地時の垂直速度を0にする。靴の回転も接地で連続する小さな角度へ変更。深く折れる固定長IKをやめ、短いデフォルメ脚の膝を股関節と足首の間で滑らかに案内する表示方式へ変更。脚画像は線分に合わせて伸縮するため、厳密な骨長保存ではない。

一周期の足送りは8→10px。接地と移動速度の関係を維持した結果、確認エリアの移動速度も約50.5→38.5px/秒になった。本編の速度や性能は変更していない。旧版は `reference-motion-fast.html`。素材生成なし。ブラウザーで改訂版の読込、後退姿勢とコマ送りの中間姿勢を目視。自然さのユーザー受入は未確認。下記の0.36秒と固定長の到達距離検証は旧版の記録で、現行の合格根拠に流用しない。

[移動と照準の独立プレビュー](reference-motion.html)／[確認画像](reference-motion-review.png)。約56pxでエリア内を移動し、同じ姿勢を3倍で併記。自動デモ、WASD/矢印移動、ポインターの左右で構え変更、場面選択、低速、一時停止、1/8周期送り、上下動比較を実装。側面の既存素材で検証可能なため、この工程の追加生成は0件。

参照：ユーザー提供の `レコーディング 2026-09-25 154654.mp4`（21.9秒、1252×782）。初回ブラウザー抽出は同一フレームになり「動画が静止」と誤判断した。その後PyAVで656フレームのH.264を直接デコードし、動作を確認。8.5〜9.5秒の連続画像から、右移動中の左への構え直し、武器姿勢を保った足の入替を設計の参考にした。動画は正しく動いており、録画側の不備ではない。参照動画はゲーム配布素材へ含めない。

変更：移動ベクトルと左右の構えを独立。左右反転のローカル座標へ移動方向を変換して脚を送るため、後退でも銃の方向を保持する。上下・斜めへの移動にも足の軌道を割り当てる。周期0.42→0.36秒、歩幅片振幅6→4px、足上げ6→3.6px、胴体上下片振幅1.45→0.9px、頭は遅延した72%の上下動。これらはリナ用の調整値で、参考動画の厳密な計測結果ではない。

再現：`build_reference_motion.py` は前の `run-review.template.html` を基にコードを組み替え、`reference-motion.template.html` と画像内包HTMLを生成する。既存素材 `run-parts-key.png` を使用。前試作は比較用に維持。

検証：24方向×1000位相×左右脚の48000サンプルで、脚の到達距離最大10.519pxが上限11.6px内。ブラウザーで読み込み・自動デモ・右移動/左構え・一時停止・場面選択・手動キー入力への切替を確認。数値検証は美術品質の合格を意味しない。

未対応：正面/背面の構えと専用パーツ、自由角度の銃口、射撃、回避、本編接続。側面の左右反転で上下方向にも移動できるが、全方向の素材が完成したわけではない。膝の接合、切替時の重心、足と床の滑りは継続レビュー。ユーザー採用は未確認。

## 通常移動を走行へ変更（最新：側面試作済み）

[走行プレビュー](run-review.html)／[確認画像](run-review.png)。周期0.42秒、構えた銃の角度を維持、左右反転、走行/停止、一時停止、1/8周期送り、低速、上下動の有無、関節と接地表示。右側面の動き確認用で、本編・前後・ストレイフ・射撃・回避は未接続。

最新のユーザー指示：通常移動は歩行ではなく常に走るイメージ。側面から設計を修正し、接地・沈み込み・蹴り出しと上下動を同期させる。前の歩行4姿勢は通常移動に採用しない。

不足していた右側面の頭、胴体、銃を構えた両腕、太腿、下腿と靴、スカーフを内蔵imagegenで生成。背景のにじみが初回と透過修正に残ったため、3回目でマゼンタ背景へ修正して抽出した。原本は `run-parts-draft.png`、`run-parts.png`、`run-parts-key.png`。生成指示は [部品](run-parts-prompt.txt)と[背景修正](run-parts-fix-prompt.txt)。追加API設定変更なし。

`run-review.template.html` が切出し矩形・クロマキー条件・パーツ寸法・関節と動作のレシピ。1536×1024の原画からRとBがG×1.8+35を超える画素を透明にし、部品の透明余白を除く。下腿と靴は別の領域で取り出し、接地時の靴底を水平に保つ。`build_run_review.py` で画像内包HTMLを再生成できる。頭・胴体をコマ別に生成しない。銃は表示検証用で本編の武器性能とは無関係。

走行設計：周期0.42秒、各足の接地率42%、左右半周期ずらし、接地中はx=6→-6、回収中は滑らかに前へ戻し最大6px持ち上げ。腿と脛は各5.8pxの二関節計算。胴体の上下動は片振幅1.45px、頭は遅延させた60%の上下動。銃の回転を胴体の傾きと相殺。背景速度は接地の足送りへ同期する。

検証：`check_run_kinematics.py` で2000サンプルの到達距離（最大10.958、上限11.6）、接地時の靴底、定速時の足送りと背景速度を確認。これは試作の数値検証であり、本編実速度との一致ではない。ブラウザーで走行・停止復帰・左右反転と56px表示を確認。拡大時の膝の接合線、首元の見え方、構えた腕/銃の大きさは引き続き美術レビュー対象。ユーザー採用は未確認。脚の接合を含む最終品質・全方向の走行を合格扱いにしない。

## 方向別と歩行ポーズの確認（最新）

今回の進捗は段階1の方向別立ち姿と、段階2の側面歩行のポーズ検証まで。全方向の完成歩行・武器・回避・本編接続は未実施。ユーザー採用は未確認。

- [方向別プレビュー](direction-review.html)：正面・右・背面・左反転、方向自動切替、全方向共通倍率と56px相当の表示。[確認画像](direction-review.png)。
- [歩行ポーズ検証](walk-review.html)：側面4姿勢の仮再生、コマ送り、左右反転。[確認画像](walk-study-review.png)。完成歩行として不合格・未採用。

内蔵imagegenを3回使用：三方向原画、透過と首元の修正、側面歩行ポーズ。原本は `turnaround-draft.png`、`turnaround.png`、`walk-study.png`。プロンプトは [方向](turnaround-prompt.txt)、[修正](turnaround-fix-prompt.txt)、[歩行](walk-study-prompt.txt)。追加の無制限な生成はせず、現時点の不合格点を記録する。

方向原画の初回は背景色が残ったため不採用。修正版は1907×825 RGBA。3領域を幅の0〜35.5%、35.5〜63.5%、63.5〜100%で切り出し、alpha>32の表示範囲を測定。正面の全高を56pxにする倍率を全方向へ共通適用。原点Xは各方向の下端9%にある靴の支持範囲中央、Yは靴の最下端。髪の外接矩形中心を原点にしない。コードと再現レシピは `build_direction_review.py`。

美術レビュー：共通倍率で全高・頭の体積は概ね一致。背面のゴーグルは帯のみで、前後の区別が読み取れる。側面の首元は拡大すると耳下の小さな肌色が残るため完全解消とは扱わない。左反転では胸の印・金具・髪の非対称性が反転する。正式素材として左右反転を採用するかは未確定。スカーフの後ろの長さ・付け根も方向間で精査が必要。

歩行レビュー：短い脚でも踏み出しと足上げは判読できる。ただし姿勢1と4の近側/遠側の入替が弱く、下半身の完全な一周期になっていない。接地姿勢の靴底も傾き、姿勢ごとに髪の輪郭が微妙に変わる。完成歩行として不合格。これを全方向へ量産しない。

仮再生は4等分の領域からalpha境界を取得し、姿勢1基準の共通倍率・各コマ最下端で表示。これは接地補正や移動距離同期ではない。`build_walk_review.py` が再現コード。次の制作は固定した頭・胴の素材を使い、近側と遠側の腕脚の接地・通過・逆接地を分けた一周期の設計。欠ける手足のポーズは再生成する。段階3以降を合格扱いにしない。

先行した[再生成の完成立ち絵](standing-review.html)は方向別制作の参照。旧分割試作v1〜v3は比較履歴であり採用素材ではない。

## 完成立ち絵の再生成

内蔵imagegenで[新しい立ち絵](redraw-standing.png)を1枚生成。[最終プロンプト](redraw-prompt.txt)。リナの現行正面素材を識別点、ユーザーの追加スクリーンショットを頭身・ゲーム視点の参考に使用。原画は透過PNGで保存、既存の原画・本編素材は上書きしない。

レビュー：顎からスカーフへの接続、肩から袖の連続性、簡略化された短い脚は前の分割試作より自然。全高56pxへ統一した[確認画像](standing-review.png)でも、左右の靴と手袋が区別できる。頭と髪の比率は依然として大きく、厳密な2頭身より頭が勝つ印象。参考画像と同等のポーズ・動きが実現したとは評価しない。

立ち絵の統一感は改善したが、ユーザー採用は未確認。新しい絵のパーツ化・歩行・武器を持つ姿・別方向・本編接続は未実施。`build_standing_review.py` は原画を改変せずHTMLへ内包し、Canvasで透明余白を除いた表示範囲を測定して56px/4倍の比較を描画する。

区分：制作・比較記録。状態：正面プレビュー試作済み、未採用。本編への置換なし。
範囲：正面の立ち姿・待機・歩行。正本は[キャラ美術](../../../design/CHARACTER_BIBLE.md)と[Actor表示](../../../development/ACTOR_ANIMATION.md)。

## 制作仕様と素材台帳

- 既存リナの茶髪・ゴーグル・青緑の目・象牙色上着・錆色スカーフを維持。
- 最新の依頼は約2頭身。頭を大きく保ち、上着の横幅を絞り、短い手足が判別できる形にする。比較全高は56px。初回の約3頭身案は不採用。
- 今回の生成範囲は正面パーツアトラス1枚。頭、胴体、左右腕、左右脚・靴、スカーフ、髪、閉眼頭の9セル。
- 原本予定1536×1536、3×3セル。生成結果の実寸を確認して切出し矩形を記録。
- 接続点：首、肩、股関節、スカーフ根元。足底中央を組立原点とし、頭・腕・脚は隠れる接続面まで描く。
- 原本と切出し／組立レシピ、通常サイズの比較、確認状況をこのフォルダーへ保存。
- 背面・側面・回避・武器接続は今回の正面試作後の検討対象。

## 確認状況

生成済み：内蔵imagegenでアトラス1枚。実寸1254×1254、RGBA PNG。指定1536×1536とは異なるため実寸の矩形を使用した。CLIや既存API設定の変更なし。

プレビュー接続済み：頭、胴体、左右腕、左右脚（靴を含む）、スカーフの7パーツ。髪パーツと閉眼頭は生成済みだが未使用。閉眼頭は輪郭差があるため、そのまま切替しない。

確認済み：ブラウザーで待機・歩行の姿勢、切替、一時停止を確認。拡大表示と通常サイズの表示を目視。これはCanvasの独立した試作であり、Godot上の連続動作や本編実プレイの検証ではない。ユーザー採用は未確認。

## ファイルと再現手順

- [プレビュー](preview.html)：画像内包、オフラインで開ける。待機／歩行、速度、一時停止、接続点表示。
- [確認画像](walk-review.png)：通常サイズと拡大を含むブラウザー表示。
- [生成原本](atlas.png)／[最終プロンプト](prompt.txt)。参照は `assets/first-workshop/rina-directions/front.png`。
- `preview.template.html`：実寸の切出し矩形、透明余白除去、表示寸法、関節原点、描画順、動作式を保持する再加工レシピ。画像の描き直しは行わない。
- `build_preview.py`：原本と現行素材をBase64で内包するだけのパッケージ処理。リポジトリ直下で `python docs/art/reviews/rina-parts-2026-09-25/build_preview.py`。

修正版は足底中央を原点に、全高56px。頭33×34（跳ね毛込み）、胴体20×17、各腕7×12、各脚6.5×11px。頭下端はy=-22、胴体上端はy=-25、脚上端はy=-11。歩行周期0.64秒、片足の持上げ1.8px、腰上下0.85px、頭の遅れ0.45rad、スカーフの遅れ0.9rad。待機／歩行を指数補間する。髪は頭に含まれる描画を使用。

## 残確認

- 新しい体型と通常サイズでの顔の読みやすさのユーザー受入。
- 腕は肩から手まで、脚は股関節から靴までの一体パーツ。肘・膝を曲げる差分は未制作。
- 背面・側面・回避・照準／武器・Godotへの接続は未実施。
- まばたきと髪単独の揺れは未接続。今回の歩行は正面その場歩きで、実移動距離との接地同期は未実施。

## 首の修正とレビュー（v3）

首をほぼ見せない、顎をスカーフにつなげるという追加依頼に対応。頭画像の下端7%（首の描画）をCanvasのソース領域から除外し、残りは元の倍率と位置で描画。顎と襟の重なりを確保し、頭の独立した回転は維持。生成原本は変更せず、追加生成なし。修正前レシピは `preview-v2.template.html`。

待機と歩行の複数姿勢、および通常サイズをブラウザーで目視した。確認した姿勢では首の肌色の帯が露出せず、顎とスカーフの接続が改善。単に胴体を前面に出す案は襟口が目立ったため採用していない。確認画像は [v3](walk-review-v3.png)。全フレームの自動検査やGodot上の確認ではない。

レビュー結論：首の接続は改善。服の細部、足の接地と蹴り出し、表情、武器を含む斜め向きのポーズは引き続き未完成。この局所修正をキャラクター全体の美術合格として扱わない。本編未接続・ユーザー受入待ち。

## 2頭身への修正（2026-09-25）

ユーザーの参考画像は頭身とシルエットの参考として扱い、画像内の文字は指示として扱っていない。既存アトラスを再配置し、追加生成なし。前回の脚長案のレシピは `preview-v1.template.html` に保存。現行プレビューは大きな頭と短い手足の版。ブラウザーで待機・歩行の表示を目視確認。修正版の確認画像は [walk-review-v2.png](walk-review-v2.png)。本編未接続・ユーザー受入待ち。
